# Option A (recommended): do NOT set VITE_API_URL on Vercel.
# The app uses baseURL /api, and client/vercel.json rewrites to Render.
#
# Local dev: leave unset — Vite proxy handles /api → http://localhost:5001
