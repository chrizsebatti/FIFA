# Optional override for production (Vercel). Default is https://fifa-sl3t.onrender.com/api
# VITE_API_URL=https://fifa-sl3t.onrender.com/api

# Local dev: leave unset — uses /api via Vite proxy → localhost:5001
